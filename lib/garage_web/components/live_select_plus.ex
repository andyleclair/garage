defmodule GarageWeb.Components.LiveSelectPlus do
  use GarageWeb, :live_component

  @impl true
  def render(assigns) do
    ~H"""

    """
  end

  @impl true
  def mount(socket) do
    {:ok, socket}
  end
end
